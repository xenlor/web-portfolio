# web-portfolio

Mi portfolio: [xenlor.dev](https://xenlor.dev)

React + Vite + Tailwind.

```bash
npm install
npm run dev              # local
bash scripts/deploy.sh   # compila y publica
```

El contenido (experiencia, proyectos, stack) está en `src/data/data.jsx`.
